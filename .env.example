# 复制本文件为 .env 并填写真实值，切勿将 .env 提交到版本控制。

# 通义千问 API 配置（必填）
QWEN_API_KEY=your_api_key_here

# API 基础地址（可选，默认为阿里云 DashScope）
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 使用的模型名称（可选）
QWEN_MODEL=qwen-vl-max

# 单次请求最大输出 token 数（可选）
QWEN_MAX_TOKENS=1024
